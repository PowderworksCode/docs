{
  "name": "powderworks-docs",
  "version": "0.1.0",
  "description": "Turn a tree of markdown into a tree of indexes. One small script to copy a code block, another to open the index on a narrow screen.",
  "type": "module",
  "bin": {
    "powderworks-docs": "./src/cli.mjs"
  },
  "files": [
    "src",
    "theme.css",
    "tools"
  ],
  "scripts": {
    "selfcheck": "node src/cli.mjs build example/content --out /tmp/pw-docs-selfcheck --site example --static example/public"
  },
  "dependencies": {
    "highlight.js": "^11.12.0",
    "marked": "^16.4.1",
    "smol-toml": "^1.8.0"
  }
}
