{
  "name": "powderworks-docs-example",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf out && node ../src/cli.mjs build content --out out --site example --static public",
    "dev": "bun run build && wrangler dev",
    "preview": "bun run build && wrangler versions upload",
    "deploy": "bun run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
